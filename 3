// Write a program to print value of a variable, address of the variable, and value stored in the pointer.

#include <iostream>
using namespace std;

int main() {
    int x = 50;
    int* ptr = &x;

    cout << x << endl;
    cout << &x << endl;
    cout << ptr << endl;
    cout << *ptr << endl;

    return 0;
}